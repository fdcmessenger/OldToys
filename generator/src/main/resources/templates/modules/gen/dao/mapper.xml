<?xml version="1.0" encoding="utf-8"?>
<template>
    <name>mapper</name>
    <filePath>src/main/resources/${lastPackageName}/${moduleName}/${subModuleName}/dao</filePath>
    <fileName>${ClassName}Dao.xml</fileName>
    <content><![CDATA[
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${packageName}.${moduleName}.dao<#if subModuleName != "">.${subModuleName}</#if>.${ClassName}Dao">
       <resultMap id="BaseResultMap" type="${packageName}.${moduleName}.entity.${ClassName}">
        <#assign columnField>
            <#list table.columnList as c>
                <#if c.isPk?? &&c.isPk==true>
                    <id column="${c.name}" property="${c.javaField}" />
                <#else>
                    <result property="${c.javaField}" column="${c.name}" />
                </#if>
            </#list>
        </#assign>
        ${columnField}
    </resultMap>
</mapper>]]>
    </content>
</template>