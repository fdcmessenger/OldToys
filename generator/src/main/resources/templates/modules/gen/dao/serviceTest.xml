<?xml version="1.0" encoding="utf-8"?>
<template>
    <name>serviceTest</name>
    <filePath>src/test/java/${packageName}/${moduleName}/service/${subModuleName}</filePath>
    <fileName>${ClassName}ServiceTest.java</fileName>
    <content><![CDATA[
/**
 * Copyright &copy; 2012-2016 <a href="https://github.com/thinkgem/jeesite">JeeSite</a> All rights reserved.
 */
package ${packageName}.${moduleName}.service<#if subModuleName != "">.${subModuleName}</#if>;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.jdbc.Sql;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.transaction.annotation.Transactional;
import ${packageName}.${moduleName}.entity<#if subModuleName != "">.${subModuleName}</#if>.${ClassName};

/**
 * ${functionName}Service接口单元测试
 * @author ${functionAuthor}
 * @version ${functionVersion}
 */
@Transactional(readOnly = false)
@RunWith(SpringRunner.class)
@SpringBootTest
//@Sql({"classpath:/${ClassName}TestInit.sql"})//delete all and insert test data
public class ${ClassName}ServiceTest {

    @Autowired
    ${ClassName}Service service;


    @Test
    public void testSelectAll() {
        List r =service.findAll();
        Assert.assertTrue(r.size()>0);
    }

}
]]>
    </content>
</template>