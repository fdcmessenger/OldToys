<?xml version="1.0" encoding="utf-8"?>
<template>
    <name>daoTest</name>
    <filePath>src/test/java/${packageName}/${moduleName}/dao/${subModuleName}</filePath>
    <fileName>${ClassName}DaoTest.java</fileName>
    <content><![CDATA[
/**
 * Copyright &copy; 2012-2016 <a href="https://github.com/thinkgem/jeesite">JeeSite</a> All rights reserved.
 */
package ${packageName}.${moduleName}.dao<#if subModuleName != "">.${subModuleName}</#if>;

import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.jdbc.Sql;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.transaction.annotation.Transactional;
import ${packageName}.${moduleName}.entity<#if subModuleName != "">.${subModuleName}</#if>.${ClassName};

/**
 * ${functionName}DAO接口单元测试
 * @author ${functionAuthor}
 * @version ${functionVersion}
 */
@Transactional(readOnly = false)
@RunWith(SpringRunner.class)
@SpringBootTest
//@Sql({"classpath:/${ClassName}TestInit.sql"})//delete all and insert test data
public class ${ClassName}DaoTest {

    @Autowired
    ${ClassName}Dao dao;


    @Test
    public void testSelectAll() {
        List r =dao.selectAll();
        Assert.assertTrue(r.size()>0);
    }

}
]]>
    </content>
</template>